apiVersion: apps/v1
kind: Deployment
metadata:
  name: qr-generator-rest
  labels:
    app: qr-generator-rest
    service-type: rest
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qr-generator-rest
  template:
    metadata:
      labels:
        app: qr-generator-rest
        service-type: rest
        tier: backend
    spec:
      containers:
      - name: qr-generator
        image: trabalho1-qr-generator-rest:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8082
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "8082"
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: pspd-config
              key: LOG_LEVEL
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 40
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
