apiVersion: v1
kind: Service
metadata:
  name: web-client
  labels:
    app: web-client
    service-type: frontend
spec:
  selector:
    app: web-client
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30000
  type: NodePort
